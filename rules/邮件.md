# 邮件发送规则

当用户输入包含关键字“发邮件”或“email”（不区分大小写）时，必须遵守以下要求：

1. **附件清单**：
   - 韩语单词本：`standalone_kr_vocab.html`
   - 日语单词本：`standalone_jp_vocab.html`

2. **收件人地址**：
   - `evelyn410927@163.com`

3. **邮件主题与正文**：
   - **主题**：`最新韩语与日语单词本HTML文件备份 [当前时间]`
   - **正文**：包含韩语与日语单词本的最新更新状态、版本号以及卡片总数。

4. **发送处理**：
   - 自动获取 `standalone_kr_vocab.html` 与 `standalone_jp_vocab.html` 两个文件的最新绝对路径。
   - 调用 Python 或系统邮件服务/脚本将两个 HTML 文件作为附件，打包并发送至指定邮箱 `evelyn410927@163.com`。
