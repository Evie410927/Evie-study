# 韩语学习项目

所有对话一律使用中文。

## 触发规则

根据用户输入判断场景。命中以下场景时，必须先完整读取对应规则文件，再按照其中的要求响应：

- 含“解释” → 读取 `rules/解释.md`
- 含“翻译” → 读取 `rules/翻译.md`
- 含“润色” → 读取 `rules/润色.md`
- 含“区别” → 读取 `rules/区别.md`
- 含“表达” → 读取 `rules/表达.md`
- 输入是一个或多个韩文单词，且不含上述关键词 → 读取 `rules/区别.md`
- 其他情况（普通中文对话等）→ 正常对话，不触发任何规则

如果同一输入同时命中多个关键词，读取所有命中的规则文件，并同时遵守；若要求冲突，以与用户主要请求最直接相关的场景规则为准。

规则文件均按 UTF-8 编码读取。不要修改这些规则文件，除非用户明确要求修改规则。
