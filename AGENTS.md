# 韩语学习项目

所有对话一律使用中文。

## 触发规则

根据用户输入判断场景。命中以下场景时，必须先完整读取对应规则文件，再按照其中的要求响应：

- 含“解释” → 读取 `rules/解释.md`
- 含“翻译” → 读取 `rules/翻译.md`
- 含“润色” → 读取 `rules/润色.md`
- 含“区别” → 读取 `rules/区别.md`
- 输入为中文或韩文内容，并在内容后附加“日语”或“일본어”作为转换标记 → 读取 `rules/日语.md`
- 输入是可明确判断的日语单词、短语、句子或语法 → 读取 `rules/日语.md`
- 含“表达”，且未使用“日语”或“일본어”作为转换标记 → 读取 `rules/表达.md`
- 含“发邮件”或“email”（不区分大小写）→ 读取 `rules/邮件.md`
- 询问 IT 技术知识、术语含义、组件关系、系统架构、运行机制或故障原理，即使不含“解释” → 读取 `rules/解释.md`
- 含“单词本自动化测试”或“note auto test”（不区分大小写）→ 运行 `python test_vocab_apps.py` 自动化测试脚本并汇报结果
- 含“检查测试点” → 深度检查 `standalone_kr_vocab.html` 最新修改部分，对比 `test_vocab_apps.py` 自动化测试脚本，补充遗漏测试点至测试脚本并运行汇报结果
- 输入是一个或多个韩文单词，且不含上述关键词 → 读取 `rules/区别.md`
- 其他情况（普通中文对话等）→ 正常对话，不触发任何规则

如果同一输入同时命中多个关键词，读取所有命中的规则文件，并同时遵守；若要求冲突，以与用户主要请求最直接相关的场景规则为准。“中文或韩文内容 + 日语/일본어”场景始终以 `rules/日语.md` 为准，不读取 `rules/表达.md`。


规则文件均按 UTF-8 编码读取。不要修改这些规则文件，除非用户明确要求修改规则。

## 项目特定约定

- **邮件发送规则**：当用户输入包含关键字“发邮件”或“email”（不区分大小写）时，必须将当前最新的两个单词本 HTML 文件（`standalone_kr_vocab.html` 和 `standalone_jp_vocab.html`）作为附件添加，并发送至指定邮箱 `evelyn410927@163.com`。
- **双端同步更新与样式功能强制一致铁律**：后续凡是用户提出“给 html 加功能”、修改样式或更新单词本应用时，**必须同时**同步更新 `standalone_kr_vocab.html`（韩语单词本）和 `standalone_jp_vocab.html`（日语单词本）两个文件！两边的 CSS 样式、页面布局、HTML 结构、DOM ID、交互逻辑与功能组件**必须 100% 完全相同并保持双向镜像同步**！
  - **卡片底部 Tag 栏与操作按钮栏强保留铁律**：单词列表中每一个单词卡片底部，**必须 100% 完整保留底部的 Tag 标签栏（包含 `#词性` 标签、自定义 Tag 及 `+` 加 Tag 按钮）和操作按钮栏（包含 `🔊` 发音朗读、`✅ 掌握 / 🔄 学习中` 状态切换、`✏️` 编辑、`🗑️` 删除）**！绝对严禁删减、隐藏或裁切卡片底部的 Tag 栏与操作按钮栏！
  - **唯一特例**：日语单词本（JP）的单词卡片和详情弹窗中允许展示韩文对应表达（`krMeaning` Badge，如 `도대체`），除此之外两端样式与功能 100% 保持完全一致！
  - **底部分页条与 Tab 栏极紧凑间距 100% 镜像统一铁律**：分页工具条（`#paginationBar`）必须且只能作为内联节点（`class="pagination-bar word-list-inline-pagination"`）放置在 `#wordList` 容器内部的最末尾，其 `style` 统一设为 `margin-top: 0px; margin-bottom: 2px;`，`#wordList` 的 `padding-bottom` 统一设为 `62px !important`。韩语（KR）与日语（JP）两端的底部分页条位置、HTML 嵌套结构与样式间距**必须 100% 完全相同并随时保持双向镜像统一**，绝不允许单端样式错位或留白不一致！
- **对话框发词/表达加入单词本规则**：只有当用户在对话窗口发送韩语/日语的**单词、词组或惯用表达**，且**输入中明确包含关键字“单词本”、“note”、“노트”或“ノート”（不区分大小写）**时，才触发加词逻辑：
  1. 自动判断发送的内容是韩语还是日语；
  2. **校验与防错**：首先进行拼写与语法校验。若发现用户写的单词/惯用表达**有拼写错误、错别字或误写**：
     - ❌ **绝对不要直接加入单词本**！
     - ⚠️ **必须立刻明确提醒用户存在错误**，指出具体错在哪里，给出正确拼写/表达建议，请用户重新确认。
     - **防重复校验**：若单词/表达拼写无误，但**对应单词本（HTML/数据集）中已经存在该词**：
       - ❌ **绝对不要直接重复加入**！
       - ⚠️ **必须立刻提醒用户该词已存在于单词本中**，并展示已有卡片内容，请用户重新确认是否仍需更新或重复添加。

  3. 若校验无误（或用户确认纠正后）：
     - 查询其准确的中文释义、实用例句及例句翻译；
     - 标注准确的发音（发音正位与字段隔离铁律）：
       - **发音正位 (`reading` 字段)**：**必须且仅能**标注真正的**实际音变/标准读音**（如 `녹녹하다` 标记 `[농녹하다]`，`막간을 이용하다` 标记 `[막까늘 이용하다]`，`짬을 내다` 标记 `[짜믈 내다]`，`걸핏하면` 标记 `[걸피타면]`（收音ㅅ+ㅎ激音化），`악랄하다` 标记 `[앙날하다]`（鼻音化），`탁월하다` 标记 `[타궐하다]`（连音化））。标题旁边的括号绝对不能误填未变音的原字！
       - **释义纯净 (`meaning` 字段)**：**必须且仅能**存放干净纯粹的词性与中文释义（如 `v. 利用幕间/空隙时间`），**绝对严禁**将发音括号 `[...]` 或拼音混入释义字段中！
       - **日语**：必须标记假名发音及音调（如 `思い出` 标记假名 `[おもいで]`）；
     - **Tag 标签打法规则**：清空所有“高级/高频”等无用 Tag。去除“单词/表达”类型 Tag，卡片只保留一个按准确词性标注的 Tag（如 **`动词`**, **`形容词`**, **`名词`**, **`副词`**, **`短语`**, **`惯用句`** 等）。


- **严禁占位符、模板化数据与未翻译原文残留铁律**：在生成或录入单词本卡片数据时（不论是单个词还是批量词汇），**绝对严禁**生成或使用任何占位符文本、通用模板句（例如 *“请准确地活用XX这个表达”*、*“XX核心词汇”*、*“具有XX色彩的”*、*“发生了XX的情况”*、*“以此事件为契机进行了XX”* 等）。所有卡片的中文释义和例句翻译**必须使用纯正地道的中文，绝对严禁在中文翻译或释义中残留未翻译的韩文或日文单词**！
- **绝对禁止正则裁切与降级拼接铁律**：严禁在清洗或修复词库数据时使用任何形式的正则字符串裁切、硬性词替换或降级拼接脚本（例如将 *“具有능청色彩的”* 裁切替换为 *“的”*，导致生成 *“adj. 的”* 或 *“他的行为表现得很的。”* 等畸规废句）。**每一个词条的词性、释义、发音、例句与例句翻译必须全量经过人工核对或显式 Naver 字典字典树精准映射，硬性确保每一个字词都有完整地道的中文释义与真实自然的中文例句翻译**！
- **新规则全量重扫描与追溯校验铁律**：后续无论何时更新、新增或修正任何关于单词本卡片字段格式、发音、释义、例句、翻译或标签的检查规则，**必须立刻在变更当下，对 `standalone_kr_vocab.html`（韩语单词本）和 `standalone_jp_vocab.html`（日语单词本）两个文件中的全量已有单词数据进行一次完整的追溯性全量扫描与排查修复**，确保已有数据集 100% 符合最新规则要求，绝不留下任何旧规则漏洞或历史脏数据！
- **对话回复硬换行与例句紧贴分行铁律**：在对话框输出任何词条、例句与中文翻译时，所有多行排版的标题、词条信息行（如词条名、词性+中文释义、实际读音、差异说明等）以及韩文/日文原句末尾**必须显式加上 `<br>` 标签**（**绝对严禁在 `<br>` 后面叠加两个空格 `  `**，防止渲染器叠加触发两次换行产生空行隙）！强制确保各信息行与外文原文独占一行，中文翻译紧贴独占下一行（中外文之间 0 空行）！两组例句之间**必须且仅能使用标准 Markdown 空行（连续两个回车 `\n\n`）**分隔，**绝对禁止使用 `ㅤ`（U+3164）等隐形占位字符**，彻底防止文本被 Markdown 渲染器压塌合并到同一行！
- **卡片复习相近表达聚类规则**：单词本（`standalone_kr_vocab.html` 与 `standalone_jp_vocab.html`）的“卡片复习”模块出词时，算法必须根据单词/表达的中文释义与标签进行语义相似度聚类，**尽量将意思相近/表达相似的卡片分组按顺序排列出词**，帮助用户对比区分记忆。
- **多例句展示与多义全面覆盖铁律**：所有单词/表达的卡片数据**必须包含至少三组地道实用例句**。若一个单词/表达包含多种释义或词性（如兼具动词与名词，或有多个不同含义），**必须确保每一个含义/词性均至少配备一组地道例句及其中文翻译**！在单词列表点击卡片主体唤起 Pop 详情弹窗以及在“卡片复习”翻面时，**必须全量展示所有例句与翻译**，严禁只截取一组例句！
- **新增词汇实时相近表达与字形/易混推荐双重联动规则**：后续无论何时向单词本（`standalone_kr_vocab.html` 与 `standalone_jp_vocab.html`）中添加或更新新单词/表达，必须确保应用程序中的 `getSimilarWords` 算法与视图组件实时动态更新！推荐算法**不仅包含意义表达上的语义相似推荐，还必须引入字形/拼写长得像、包含相同汉字/谚文/假名、或带有 `#易混` 标签的形似词双重推荐机制**！新词自动纳入相似度交叉匹配池，在卡片 Pop 详情弹窗与“卡片复习”翻面背面**实时推荐并展示含义相近或字形易混的关联卡片**！
- **对话上下文驱动的相近/易混关系联动铁律**：后续在用户明确使用“单词本”、“note”、“노트”或“ノート”触发加词/表达时，必须把**当前对话中已经出现的比较、区别、替换、近义、反义、字形相似及共同使用场景**作为判断关联词的重要信号，与中文释义、词性、字形/拼写、标签及 `getSimilarWords` 算法结果合并评估，不能只依据单张卡片的孤立字段判断。
  1. **双词区分即强易混信号**：只要用户在同一对话中同时给出两个或多个单词/表达并要求“区别、区分、对比、辨析”，这些词之间必须默认判定为用户实际学习中的**强易混关系**；但若当次输入没有加词触发关键字，仍然不得擅自加入单词本。
  2. **加入 note 时双向持久关联**：当上述词中的任意词随后被加入 note，必须检查同组词是否已存在于相应词库；已存在时必须建立可持久化的双向关联，使 A 能推荐 B、B 也能推荐 A，不能只依赖当前聊天记忆或单向推荐。另一词尚未入库时，必须保留该对话关系，并在其以后入库时补齐双向关联。
  3. **推荐优先级**：用户明确要求过区分/辨析的词对属于最高优先级的易混候选；对话中明确提到的近义、替换或相关表达属于强相关候选；普通共同出现只作为辅助信号，仍须结合语义与用法核验，严禁把偶然同现但无实际关联的词强行推荐。
  4. **统一展示**：由对话上下文确认的关联词必须进入卡片 Pop 详情弹窗和“卡片复习”背面的“相近表达/近义推荐”区域，并参与复习聚类排序；若属于易混词对，还必须同步纳入易混推荐逻辑。
- **DOM空安全守护与数据装载防空保底铁律**：后续修改或更新应用程序代码时，必须强制遵守极严苛的防卡死鲁棒性范式：
  1. **DOM空安全保护**：所有 DOM 节点获取与属性赋值必须全量经过空指针安全防护（如 `const el = document.getElementById(...); if (el) el.textContent = ...`），严禁任何未经判空的解引用导致 JS 线程中途抛出 `TypeError` 停摆假死；
  2. **数据装载防空保底**：存储读取（`loadData()`）必须包含无条件保底防空逻辑：一旦检测到单词数组为空 `[]` 或非数组，必须无条件自动触发 `loadSampleData(false)` 将内置全量样本卡片直接补充装载，硬性确保页面绝不呈现无理由的空白；
  3. **环境沙盒降级与防崩溃防护**：全局脚本必须注入 `SafeStorageWrapper` 存储降级屏障与全局 `window.onerror` 守护，即便在 iOS Safari `file://` 沙盒或严苛移动端浏览器下，也硬性保障卡片 100% 渲染且所有交互按钮正常流畅响应。
- **搜索精确匹配规则（字段隔离铁律）**：在单词本应用（`standalone_kr_vocab.html` 和 `standalone_jp_vocab.html`）的关键词搜索框（`searchInput`）中进行检索过滤时，**仅匹配单词/表达本身（`word`）、发音/假名/读音（`reading`）以及对应的准确中文或韩文/日文释义（`meaning`）**！**绝对严禁**将例句（`example` / `exampleTrans` / `examples`）、标签（`tags`）或笔记（`notes`）等辅助扩展字段纳入关键词搜索匹配范围，确保搜索结果精确纯净。
- **单词本自动化测试触发与动态扩展规则**：
  1. 当用户输入包含关键字“单词本自动化测试”或“note auto test”（不区分大小写）时，必须立刻运行 `python test_vocab_apps.py` 自动化测试脚本，全面检测韩语与日语单词本（`standalone_kr_vocab.html` 与 `standalone_jp_vocab.html`）的卡片宽度自适应、底部Tab导航、复习控制按钮（📌易忘/🔀易混/✅记住了）、主子筛选联动、分页与跳页选择器、自动置顶及全量 DOM 空安全等关键功能点，并向用户汇报测试结果；
  2. **“检查测试点”专属触发逻辑**：当用户输入包含关键字“检查测试点”时，必须重新扫描并检查 `standalone_kr_vocab.html` 里的最新修改部分与各项功能细节，对比现有的 `test_vocab_apps.py` 自动化测试脚本，一旦发现有遗漏的要点或未覆盖的测试断言，必须立刻补充追加至测试脚本（确保韩语 KR 与日语 JP 双端 100% 镜像同步），并运行自动化测试汇报比对与更新结果；
  3. **测试点动态追加机制**：后续凡是用户反馈的 Bug（如点击没反应、点不动、卡片宽度问题、页面跳页等）或明确指示“xxx加入测试点”，**必须同步将该测试点追加更新至 `test_vocab_apps.py` 脚本的断言矩阵中**，确保自动化测试持续覆盖最新风险点！
  4. **双端测试点完全对齐铁律**：除非用户明确说明是 JP 或 KR 的专属测试点，否则后续凡是添加、补充测试点、处理 Bug 反馈或编写功能断言时，**必须同时且同步应用于 `standalone_kr_vocab.html`（韩语单词本）和 `standalone_jp_vocab.html`（日语单词本）两端**！`test_vocab_apps.py` 中的测试套件必须保持韩语与日语两端检查点数量、断言标准与测试逻辑 100% 镜像对称与完全一致，绝不允许单端遗漏或滞后！
- **页面垂直滚动条与 overflow-y 滚动畅通铁律**：页面与容器 CSS 绝对禁止设置硬性的 `overflow: hidden` 锁定 `html, body` 页面高度。必须强制保持 `overflow-y: auto !important` 与 Webkit 优雅精致的滑动条样式（`::-webkit-scrollbar`），保证页面最右侧垂直滑动条始终可见流畅，用户随时可以上下滑动浏览全量词条。
- **自定义标签行内打字输入铁律**：卡片底部的自定义标签增加功能，绝对禁止使用弹出 Pop 窗口或 Prompt 弹窗的形式。必须保持在原标签位置直接展开高亮行内打字框（`showInlineTagInput`），支持敲击 Enter 或失焦 blur 直接提交保存！

- **统计数字与卡片数据一致性强绑定与自我修复铁律**：为防止出现“卡片列表有词，但顶部统计按钮却显示 (0)”的数据错位 Bug，必须强制遵守以下防范规则：
  1. **统计计算自我修复（Self-Healing Stats）**：在 `updateStats()` 统计函数计算数字时，如果发现内存数组 `this.words` 为空 `[]` 或计算出的数字为 0，而当前 DOM 列表中存在预渲染卡片，**必须无条件触发自动自我修复逻辑（Auto Self-Heal）**，从 `samples` 内置数组恢复数据，硬性确保“卡片数量与顶部数字 100% 绝对实时对应”；
  2. **静态 HTML 结构与内置数据集数字同步锁定**：每次往 `samples` 中新增或删除词条时，必须使用自动化构建脚本**同步更新 HTML 结构中 `<span id="count-all">` 和 `<span id="count-learning">` 的默认静态数字标记**，确保即使在没有任何 JS 运行的 QuickLook 静态预览框中，数字也 100% 准确对应！

- **新增词条完成后自动部署上线规则**：今后只要依据“单词本”、“note”、“노트”或“ノート”触发并成功新增词条，必须在本地数据、失效保护数据与静态统计数字全部更新后，立即运行完整的 `python test_vocab_apps.py` 自动化测试。仅当完整测试全部通过时，自动暂存本次相关文件、创建 Git 提交并推送到 GitHub 远端 `github/main`，随后确认远端提交和 GitHub Pages 在线页面已经包含新增词条；无需等待用户再次要求“推送”。若测试未通过或部署失败，严禁推送不完整版本，必须保留本地改动并立即向用户报告具体失败项。






