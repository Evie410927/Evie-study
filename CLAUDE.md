# 韩语学习项目

所有对话一律使用中文。

## 触发规则

根据用户输入判断场景，读取对应规则文件后再响应：

- 含"解释" → 读取 rules/解释.md
- 含"翻译" → 读取 rules/翻译.md
- 含"润色" → 读取 rules/润色.md
- 含"区别" → 读取 rules/区别.md
- 含"表达" → 读取 rules/表达.md
- 输入是韩文单词（一个或多个），且不含上述关键词 → 读取 rules/区别.md
- 其他情况（普通中文对话等）→ 正常对话，不触发任何规则
