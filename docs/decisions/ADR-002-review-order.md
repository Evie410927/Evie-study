# ADR-002：复习卡片使用四种显式排序

- 状态：Accepted
- 日期：2026-09-24

## 决策

复习页只提供以下四种排序：

1. `createdDesc`：近 → 远，默认值。
2. `createdAsc`：远 → 近。
3. `desc`：五星 → 无星。
4. `asc`：无星 → 五星。

复习出卡不执行自动相似表达聚类。相近表达仅使用用户手动维护的双向关系展示，不隐式改变复习顺序。

## 验证

- 契约验证器检查选项集合、顺序和默认值。
- Selenium 为四种排序分别验证真实出卡顺序。
